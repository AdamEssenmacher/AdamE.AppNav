# Getting Started sample

This is the smallest supported MAUI onboarding path: two generated typed routes, two generated pages,
a stack navigation model, a typed startup fallback, and native Back reconciliation. It intentionally
contains no external-ingress or deferred-persistence configuration.
