namespace AdamE.MauiRouter.Planning;

/// <summary>
/// Controls when a contextual stack mutation is allowed to reuse the current stack.
/// </summary>
public enum ContextualStackEligibility
{
    MatchingScope,
    AnyScope
}
